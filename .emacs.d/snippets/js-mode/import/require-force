# -*- mode: snippet -*-
# name: import require force
# key: imrqforce
# --
function requireForce( id ) {
  id = require.resolve( id );
  var old = require.cache[ id ];
  delete require.cache[ id ];
  result = require( id );
  require.cache[ id ] = old;
  return result;
}