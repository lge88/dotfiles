# -*- mode: snippet -*-
# name: function ball
# key: funball
# --
function ball( r ) {
  var m = new THREE.Mesh(
    new THREE.SphereGeometry( r || 40 )
  );
  m.material.wireframe = false;
  return m;
}$0