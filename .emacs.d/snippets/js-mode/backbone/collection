# -*- mode: snippet -*-
# name: backbone collection
# key: bbcol
# --
var ${1:New_Collection} = Backbone.Collection.extend({
    model : ${2:Model},
    initialize: function() {
        $0
    }
});

module.exports = $1;