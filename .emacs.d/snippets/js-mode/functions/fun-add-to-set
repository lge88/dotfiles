# -*- mode: snippet -*-
# name: function addToSet
# key: funcaddToSet
# --
function addToSet( arr, item ) {
  if ( -1 === arr.indexOf( item ) ) {
    arr.push( item );
    return true;
  }
  return false;
}40