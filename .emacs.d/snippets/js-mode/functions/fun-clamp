# -*- mode: snippet -*-
# name: function clamp( a, b )
# key: funclamp
# --
function clamp( x, a, b ) {
  if ( a > b ) {
    var tmp = b;
    b = a;
    a = tmp;
  }

  if ( x < a ) {
    return a;
  } else if ( x > b ) {
    return b;
  } else {
    return x;
  }
}$0
